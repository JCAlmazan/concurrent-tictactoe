{
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "clean": "rm -rf client/out client/build server/dist server/out",
    "build": "npm run clean && npm run build --workspace client && npm run build --workspace server",
    "start": "node server/dist/index.js"
  }
}
